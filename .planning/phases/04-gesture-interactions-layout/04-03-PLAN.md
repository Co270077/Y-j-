---
phase: 04-gesture-interactions-layout
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/schedule/TaskBlock.tsx
  - src/pages/SchedulePage.tsx
  - src/components/protocols/ProtocolList.tsx
  - src/pages/ProtocolsPage.tsx
  - src/pages/MealsPage.tsx
  - src/hooks/useSwipe.ts
  - src/__tests__/ProtocolList.test.tsx
autonomous: false
requirements:
  - INTR-02
  - INTR-03
  - LAYT-02

must_haves:
  truths:
    - "User can swipe right on a schedule task to complete it"
    - "User can swipe left on schedule tasks, protocol items, and meal templates to delete with undo"
    - "Tapping a protocol card expands supplement details inline"
    - "Tapping a meal template card expands food list and macros inline"
    - "Only one item expanded at a time per list (accordion)"
    - "Undo toast appears after swipe-delete with 5-second countdown"
    - "Tapping undo restores the item"
  artifacts:
    - path: "src/components/schedule/TaskBlock.tsx"
      provides: "Swipe-right-to-complete on schedule tasks"
    - path: "src/pages/SchedulePage.tsx"
      provides: "Swipe-delete wired with undo toast"
    - path: "src/components/protocols/ProtocolList.tsx"
      provides: "Accordion expansion + swipe-delete on protocols"
    - path: "src/pages/MealsPage.tsx"
      provides: "Accordion expansion + swipe-delete on meal templates"
  key_links:
    - from: "src/components/schedule/TaskBlock.tsx"
      to: "src/components/ui/SwipeActionRow.tsx"
      via: "wraps task content"
      pattern: "SwipeActionRow"
    - from: "src/pages/SchedulePage.tsx"
      to: "src/utils/toast.ts"
      via: "showToastWithAction for undo"
      pattern: "showToastWithAction"
    - from: "src/components/protocols/ProtocolList.tsx"
      to: "motion/react"
      via: "AnimatePresence height:auto accordion"
      pattern: "AnimatePresence"
---

<objective>
Wire SwipeActionRow into all list items, add accordion inline expansion to protocols and meals, and implement the undo-delete flow.

Purpose: This is the integration plan — connect the foundation components (SwipeActionRow, BottomSheet, Toast undo) into the actual pages. Users get swipe-to-complete, swipe-to-delete with undo, and inline detail expansion.
Output: All 3 pages (Schedule, Protocols, Meals) gain gesture interactions and inline expansion.
</objective>

<execution_context>
@/Users/samarceneaux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samarceneaux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gesture-interactions-layout/04-RESEARCH.md
@.planning/phases/04-gesture-interactions-layout/04-01-SUMMARY.md
@.planning/phases/04-gesture-interactions-layout/04-02-SUMMARY.md

<interfaces>
From src/components/ui/SwipeActionRow.tsx (created in Plan 02):
```typescript
interface SwipeActionRowProps {
  children: React.ReactNode
  onComplete?: () => void
  onDelete?: () => void
  enabled?: boolean
  className?: string
}
```

From src/utils/toast.ts (extended in Plan 01):
```typescript
export function showToastWithAction(
  text: string, actionLabel: string, onAction: () => void, duration?: number
): void
```

From src/components/schedule/TaskBlock.tsx:
```typescript
interface TaskBlockProps {
  task: Task
  log: DailyLog | undefined
  onToggleComplete: () => void
  onToggleSubtask: (subtaskId: string) => void
  onEdit: () => void
  onDuplicate?: () => void
}
```

From src/components/protocols/ProtocolList.tsx:
```typescript
interface ProtocolListProps {
  protocols: Protocol[]
  onSelect: (protocol: Protocol) => void
  onToggleActive: (id: number) => void
}
```

From src/motion/transitions.ts:
```typescript
export const snappy: Transition
```

From src/stores/scheduleStore.ts:
```typescript
toggleTaskComplete: (taskId: number, day: DayOfWeek) => Promise<void>
deleteTask: (taskId: number) => Promise<void>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire swipe-to-complete and swipe-to-delete into schedule tasks</name>
  <files>src/components/schedule/TaskBlock.tsx, src/pages/SchedulePage.tsx, src/hooks/useSwipe.ts</files>
  <action>
1. **TaskBlock.tsx** — Add `onDelete` prop to `TaskBlockProps` interface. Wrap the entire task block content in `<SwipeActionRow>`:
   - `onComplete={onToggleComplete}` — swipe right completes the task
   - `onDelete={onDelete}` — swipe left triggers delete flow (handled by parent)
   - `enabled={!isComplete}` — disable swipe-to-complete on already-completed tasks (still allow swipe-to-delete via separate logic: pass onComplete only if not complete, always pass onDelete)
   - Actually: pass `onComplete={isComplete ? undefined : onToggleComplete}` and `onDelete={onDelete}` — SwipeActionRow only shows right bg when onComplete provided
   - Import `SwipeActionRow` from `'../ui/SwipeActionRow'`
   - The existing complete button stays functional (some users prefer tapping)

2. **SchedulePage.tsx** — Implement undo-delete flow:
   - Add `pendingDelete` state: `useState<{ taskId: number; timer: ReturnType<typeof setTimeout> } | null>(null)`
   - `handleSwipeDelete(taskId: number)`:
     - If `pendingDelete` exists: immediately commit previous deletion (`deleteTask(pendingDelete.taskId)`, clear timer)
     - Remove task from visible list via local filter state (soft delete): `setHiddenTaskIds(prev => new Set([...prev, taskId]))`
     - Start 5-second timer: `setTimeout(() => { deleteTask(taskId); setPendingDelete(null); setHiddenTaskIds(prev => { const next = new Set(prev); next.delete(taskId); return next }) }, 5000)`
     - Call `showToastWithAction('Task deleted', 'Undo', undoFn, 5000)`:
       - `undoFn`: clear timer, remove taskId from `hiddenTaskIds`, set `pendingDelete` to null
     - Store timer in `pendingDelete`
   - Filter `hiddenTaskIds` from rendered task list
   - On page unmount (useEffect cleanup): if `pendingDelete` exists, commit deletion immediately
   - Pass `onDelete={() => handleSwipeDelete(task.id!)}` to each `TaskBlock`
   - Remove the old `useSwipe` import and `swipeProps` spread on the Timeline wrapper (day switching via swipe conflicts with item swipe gestures — day switching stays via DaySwitcher buttons only)

3. **useSwipe.ts** — Do NOT delete yet (other components might use it). Just remove the import from SchedulePage.tsx. Add a `@deprecated` JSDoc comment to `useSwipe`.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Schedule tasks are wrapped in SwipeActionRow. Swipe right completes, swipe left triggers soft-delete with undo toast. Undo restores the task. Previous pending delete commits when new delete fires. Cleanup on unmount commits pending delete.</done>
</task>

<task type="auto">
  <name>Task 2: Add accordion expansion to ProtocolList and MealsPage with swipe-delete</name>
  <files>src/components/protocols/ProtocolList.tsx, src/pages/ProtocolsPage.tsx, src/pages/MealsPage.tsx, src/__tests__/ProtocolList.test.tsx</files>
  <action>
1. **ProtocolList.tsx** — Add accordion expansion and swipe-to-delete:
   - Add `onDelete` prop to `ProtocolListProps`: `onDelete?: (protocol: Protocol) => void`
   - Add `expandedId` state: `useState<number | null>(null)`
   - Toggle expansion on card tap: `onClick={() => toggleExpand(protocol.id!)}` where `toggleExpand(id)` sets `expandedId(prev => prev === id ? null : id)`
   - Remove `onSelect` callback from card `onClick` — replaced by inline expansion (keep `onSelect` in props for edit button inside expanded view)
   - Collapsed state (current): protocol name, supplement count, cycle pattern, active badge — KEEP as-is
   - Expanded state (NEW): wrap in `<AnimatePresence initial={false}>`. When `expandedId === protocol.id`, render `m.div` with:
     - `initial={{ height: 0, opacity: 0 }}`
     - `animate={{ height: 'auto', opacity: 1, transition: snappy }}`
     - `exit={{ height: 0, opacity: 0, transition: snappy }}`
     - `style={{ overflow: 'hidden' }}` (required for height:auto measurement)
     - Content: full supplement list with doses and timing (reuse existing supplement rendering), "Edit" button calling `onSelect(protocol)`, notes if any
   - Wrap each card in `<SwipeActionRow onDelete={() => onDelete?.(protocol)}>`:
     - No `onComplete` — protocols don't have a complete action
   - Add subtle elevation on expanded card: `shadow-lg` class when expanded

2. **ProtocolsPage.tsx** — Wire swipe-delete with undo:
   - Same undo pattern as SchedulePage: `pendingDelete` state, `handleSwipeDelete`, `hiddenIds` set, `showToastWithAction`
   - Pass `onDelete` to `ProtocolList`
   - On unmount: commit pending delete
   - Remove `ConfirmDialog` import and JSX for protocol deletion (replaced by swipe-delete + undo)

3. **MealsPage.tsx** — Add accordion expansion and swipe-delete to meal templates:
   - Add `expandedId` state for meal template accordion
   - Meal template cards: tap toggles expansion (accordion, one at a time)
   - Collapsed: name, food count, macro summary (current)
   - Expanded: full food list with individual macros, "Edit" button (opens MealTemplateEditor), macro detail
   - Wrap each template card in `<SwipeActionRow onDelete={() => handleSwipeDeleteTemplate(tpl)}>`:
   - Implement same undo pattern: `pendingDelete`, `hiddenIds`, `showToastWithAction`
   - On undo: restore template to list
   - On expire: `deleteTemplate(id)`
   - Remove `ConfirmDialog` for template deletion (replaced by swipe + undo)
   - Keep Eating Window Card and Daily Totals Card as non-swipeable (per user decision: dashboard/summary cards have NO swipe-delete)

4. **ProtocolList.test.tsx** — Update existing tests and add accordion tests:
   - Add tests (unskip from Wave 0 if stubs exist, or add new):
     - "expands protocol details on tap" — click card, verify expanded content renders
     - "collapses expanded protocol on second tap" — click again, verify collapsed
     - "only one protocol expanded at a time" — expand A, expand B, verify A collapsed
   - Keep existing tests passing
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run src/__tests__/ProtocolList.test.tsx 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Protocol cards expand inline with AnimatePresence height animation. Meal template cards expand inline with same pattern. Swipe-left-to-delete works on protocols and meal templates with undo toast. Only one item expanded at a time per list. ConfirmDialog removed from both pages for list item deletion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all gesture interactions and layout changes</name>
  <files></files>
  <action>
Human verification of all Phase 4 gesture interactions:
1. Schedule page: Swipe a task right — should complete with green reveal animation + haptic. Swipe a task left — should delete with red reveal + undo toast above bottom nav. Tap "Undo" within 5 seconds — task reappears.
2. Schedule page: Tap the FAB or header + button — task form opens as bottom sheet at half height. Drag the handle bar down — sheet dismisses. Try a fast flick down — should dismiss from any position.
3. Protocols page: Tap a protocol card — should expand inline showing supplement details. Tap another — previous collapses (accordion). Swipe left on a protocol — delete with undo toast.
4. Meals page: Tap a meal template — expands inline showing food list. Swipe left — delete with undo. Eating Window and Daily Totals cards should NOT be swipeable.
5. In any bottom sheet with long content (MealTemplateEditor): scroll content — should scroll normally. Only dragging the handle bar should dismiss.
6. Run `npx vitest run` — all tests pass.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User approves all gesture interactions are working correctly. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite green
- `npx tsc --noEmit` — no type errors
- All 5 success criteria from ROADMAP Phase 4 verified manually
</verification>

<success_criteria>
1. Swipe right on schedule task completes it (INTR-02)
2. Swipe left on any list item reveals delete with undo toast (INTR-03)
3. Protocol and meal items expand details inline via tap (LAYT-02)
4. All modals open as bottom sheets with drag-to-dismiss (INTR-04, LAYT-01 — verified from Plan 01)
5. Undo toast has 5-second countdown, positioned above bottom nav
</success_criteria>

<output>
After completion, create `.planning/phases/04-gesture-interactions-layout/04-03-SUMMARY.md`
</output>
