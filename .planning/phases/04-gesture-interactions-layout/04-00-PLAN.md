---
phase: 04-gesture-interactions-layout
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - src/__tests__/SwipeActionRow.test.tsx
  - src/__tests__/BottomSheet.test.tsx
  - src/__tests__/Toast.test.tsx
autonomous: true
requirements:
  - INTR-02
  - INTR-03
  - INTR-04
  - LAYT-01
  - LAYT-02

must_haves:
  truths:
    - "@use-gesture/react is installed and importable"
    - "Test scaffolds exist for all Phase 4 components"
  artifacts:
    - path: "package.json"
      provides: "@use-gesture/react dependency"
      contains: "@use-gesture/react"
    - path: "src/__tests__/SwipeActionRow.test.tsx"
      provides: "Swipe gesture test stubs"
      min_lines: 20
    - path: "src/__tests__/BottomSheet.test.tsx"
      provides: "BottomSheet test stubs"
      min_lines: 20
    - path: "src/__tests__/Toast.test.tsx"
      provides: "Toast undo action test stubs"
      min_lines: 10
  key_links: []
---

<objective>
Install @use-gesture/react and create test scaffolds for all Phase 4 components.

Purpose: Unblock all subsequent plans — @use-gesture/react is a hard dependency for swipe gestures. Test scaffolds define expected behavior before implementation.
Output: Installed dependency, 3 test files with stubs covering INTR-02, INTR-03, INTR-04, LAYT-01, LAYT-02.
</objective>

<execution_context>
@/Users/samarceneaux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samarceneaux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-gesture-interactions-layout/04-RESEARCH.md

@src/__tests__/Button.test.tsx (existing test pattern reference)
@src/__tests__/ProtocolList.test.tsx (will be updated in Plan 03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @use-gesture/react and create test scaffolds</name>
  <files>package.json, src/__tests__/SwipeActionRow.test.tsx, src/__tests__/BottomSheet.test.tsx, src/__tests__/Toast.test.tsx</files>
  <action>
1. Run `npm install @use-gesture/react` — verify it resolves and installs cleanly with React 19.

2. Create `src/__tests__/SwipeActionRow.test.tsx` with the following test stubs (use `it.skip` for all):
   - "renders children content" — verify SwipeActionRow renders its children
   - "calls onComplete when swiped right past threshold" — simulate pointerdown/pointermove/pointerup with clientX delta > 40% of container width; expect onComplete callback
   - "springs back when released below threshold" — simulate swipe right < 40%; verify item returns to x=0
   - "calls onDelete when swiped left past threshold" — simulate left swipe > 40%; expect onDelete callback
   - "locks direction after 10px movement" — simulate diagonal movement; verify only horizontal or vertical is active
   - "cancels gesture when vertical movement dominates" — simulate vertical > horizontal in first 10px; verify no swipe action fires
   - Set `window.innerWidth = 375` in beforeEach for consistent threshold calculation.
   - Import pattern: `import { render, screen } from '@testing-library/react'` and `import { describe, it, expect, vi, beforeEach } from 'vitest'`

3. Create `src/__tests__/BottomSheet.test.tsx` with test stubs:
   - "renders when isOpen is true" — verify children render
   - "does not render when isOpen is false" — verify no content
   - "calls onClose when backdrop clicked" — simulate backdrop click
   - "accepts detent prop and renders at correct size" — verify detent prop is accepted (peek, half, full)
   - "renders title in header" — verify title prop renders
   - Set `window.innerHeight = 800` in beforeEach for detent calculations.

4. Create `src/__tests__/Toast.test.tsx` with test stubs:
   - "renders toast text" — verify text displays
   - "renders undo action button when action provided" — verify action button renders with correct label
   - "calls action callback when undo button clicked" — verify onAction fires

All tests use `it.skip(...)` since components don't exist yet. Follow existing test file patterns from Button.test.tsx.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && node -e "require('@use-gesture/react')" && npx vitest run src/__tests__/SwipeActionRow.test.tsx src/__tests__/BottomSheet.test.tsx src/__tests__/Toast.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>@use-gesture/react installed, 3 test scaffold files exist with skipped tests, vitest collects all tests without errors</done>
</task>

</tasks>

<verification>
- `npm ls @use-gesture/react` shows installed version
- All 3 test files exist and vitest discovers them
- No import errors in test files
</verification>

<success_criteria>
@use-gesture/react v10.3.x installed. Test scaffolds for SwipeActionRow (6 tests), BottomSheet (5 tests), Toast (3 tests) exist as skipped stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gesture-interactions-layout/04-00-SUMMARY.md`
</output>
