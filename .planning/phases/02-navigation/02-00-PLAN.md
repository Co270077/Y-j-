---
phase: 02-navigation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vite.config.ts
  - src/__tests__/NavigationContext.test.tsx
  - src/__tests__/BottomNav.test.tsx
  - src/__tests__/FAB.test.tsx
  - src/__tests__/Timeline.test.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "vitest runs and exits cleanly with zero tests or pending stubs"
    - "Each test stub file imports the component/module it will test"
    - "npx vitest run completes in under 5 seconds"
  artifacts:
    - path: "vite.config.ts"
      provides: "vitest test configuration block"
      contains: "test:"
    - path: "src/__tests__/NavigationContext.test.tsx"
      provides: "Test stub for NAV-01 direction context"
      contains: "describe"
    - path: "src/__tests__/BottomNav.test.tsx"
      provides: "Test stub for NAV-04 layoutId pill"
      contains: "describe"
    - path: "src/__tests__/FAB.test.tsx"
      provides: "Test stub for NAV-02 conditional rendering"
      contains: "describe"
    - path: "src/__tests__/Timeline.test.tsx"
      provides: "Test stub for NAV-03 scroll trigger"
      contains: "describe"
  key_links:
    - from: "vite.config.ts"
      to: "vitest"
      via: "test config block with jsdom environment"
      pattern: "environment.*jsdom"
---

<objective>
Install vitest test infrastructure and create test stubs for all Phase 2 requirements.

Purpose: VALIDATION.md declares wave_0_complete: false and nyquist_compliant: false. All per-task verification commands reference vitest test files that don't exist. This Wave 0 plan installs the test framework and creates stub files so Wave 1 and Wave 2 plans can run behavioral verification after each task.

Output: Working vitest config, 4 test stub files with pending tests for NAV-01 through NAV-04.
</objective>

<execution_context>
@/Users/samarceneaux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samarceneaux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-navigation/02-VALIDATION.md
@.planning/phases/02-navigation/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and configure test environment</name>
  <files>vite.config.ts</files>
  <action>
1. Install test dependencies:
   ```bash
   npm install -D vitest @testing-library/react @testing-library/user-event jsdom
   ```

2. Add vitest test config block to `vite.config.ts`. Add the import at the top:
   ```typescript
   /// <reference types="vitest/config" />
   ```
   Then add a `test` block inside `defineConfig`:
   ```typescript
   test: {
     globals: true,
     environment: 'jsdom',
     setupFiles: [],
     include: ['src/**/*.test.{ts,tsx}'],
   },
   ```

3. Verify vitest runs: `npx vitest run` should exit 0 with "no test files found" or similar clean output.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>vitest installed, vite.config.ts has test block with jsdom environment, npx vitest run exits cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 test stub files for Phase 2 requirements</name>
  <files>src/__tests__/NavigationContext.test.tsx, src/__tests__/BottomNav.test.tsx, src/__tests__/FAB.test.tsx, src/__tests__/Timeline.test.tsx</files>
  <action>
Create `src/__tests__/` directory and 4 test stub files. Each file should have a `describe` block with `test.todo()` entries describing the behaviors that will be tested after the corresponding implementation task completes. Use `import { describe, test } from 'vitest'`.

**NavigationContext.test.tsx** (NAV-01):
```typescript
import { describe, test } from 'vitest'

describe('NavigationContext', () => {
  test.todo('provides direction ref defaulting to tab')
  test.todo('navigateTo updates direction ref before navigating')
  test.todo('useNavigation throws when used outside NavigationProvider')
})
```

**BottomNav.test.tsx** (NAV-04):
```typescript
import { describe, test } from 'vitest'

describe('BottomNav', () => {
  test.todo('renders layoutId active-tab-indicator on active tab')
  test.todo('calls navigateTo with tab direction on click')
  test.todo('renders all 4 tab buttons')
})
```

**FAB.test.tsx** (NAV-02):
```typescript
import { describe, test } from 'vitest'

describe('FAB', () => {
  test.todo('renders with correct aria-label')
  test.todo('calls onClick handler when clicked')
  test.todo('has fixed positioning above bottom nav')
})
```

**Timeline.test.tsx** (NAV-03):
```typescript
import { describe, test } from 'vitest'

describe('Timeline auto-scroll', () => {
  test.todo('scrolls to current time block on mount')
  test.todo('scrolls to next upcoming block when between tasks')
  test.todo('scrolls to last block in late night scenario')
  test.todo('applies bamboo left-border accent to scroll target')
})
```

These are stubs with `test.todo()` — they will be marked as pending (not failing) when vitest runs. Implementation plans (02-01, 02-02) will fill in the test bodies as part of their verification.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>4 test stub files exist in src/__tests__/. npx vitest run shows all tests as pending/todo. No failures. vitest exits cleanly.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — exits 0, reports 4 test suites with todo tests
- `npx tsc --noEmit` — still compiles clean (test files don't break types)
- All 4 stub files exist: NavigationContext.test.tsx, BottomNav.test.tsx, FAB.test.tsx, Timeline.test.tsx
</verification>

<success_criteria>
- vitest installed and configured in vite.config.ts with jsdom environment
- 4 test stub files exist with describe blocks and test.todo entries
- npx vitest run completes in under 5 seconds with clean exit
- Wave 1 and Wave 2 plans can now reference vitest commands in their verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-navigation/02-00-SUMMARY.md`
</output>
