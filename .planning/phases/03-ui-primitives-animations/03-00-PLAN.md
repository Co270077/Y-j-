---
phase: 03-ui-primitives-animations
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/__tests__/Button.test.tsx
  - src/__tests__/Toggle.test.tsx
  - src/__tests__/SubtaskList.test.tsx
  - src/__tests__/ProgressRing.test.tsx
  - src/__tests__/ProtocolList.test.tsx
  - src/__tests__/useCountUp.test.ts
  - src/__tests__/Skeleton.test.tsx
  - src/__tests__/Timeline.test.tsx
autonomous: true
requirements:
  - INTR-01
  - INTR-05
  - DATV-01
  - DATV-02
  - DATV-03
  - DATV-04

must_haves:
  truths:
    - "Every Phase 3 implementation task has a failing test to run against"
    - "Test suite runs green (stubs expect pre-migration state or skip)"
  artifacts:
    - path: "src/__tests__/Button.test.tsx"
      provides: "INTR-01 Button whileTap tests"
    - path: "src/__tests__/Toggle.test.tsx"
      provides: "INTR-01 Toggle motion tests"
    - path: "src/__tests__/SubtaskList.test.tsx"
      provides: "INTR-01 checkbox whileTap tests"
    - path: "src/__tests__/ProgressRing.test.tsx"
      provides: "DATV-01 m.circle tests"
    - path: "src/__tests__/ProtocolList.test.tsx"
      provides: "DATV-02 stagger wrapper tests"
    - path: "src/__tests__/useCountUp.test.ts"
      provides: "DATV-03 hook behavior tests"
    - path: "src/__tests__/Skeleton.test.tsx"
      provides: "DATV-04 shimmer component tests"
  key_links: []
---

<objective>
Create test scaffolds for all Phase 3 requirements before any implementation begins.

Purpose: Nyquist compliance — every implementation task has an automated verify command from day one.
Output: 7 new test files + 1 extended test file, all runnable via `npx vitest run`.
</objective>

<execution_context>
@/Users/samarceneaux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samarceneaux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-primitives-animations/03-CONTEXT.md
@.planning/phases/03-ui-primitives-animations/03-RESEARCH.md
@.planning/phases/03-ui-primitives-animations/03-VALIDATION.md
@src/__tests__/FAB.test.tsx
@src/__tests__/Timeline.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create component test stubs for INTR-01 and DATV-01/02/04</name>
  <files>
    src/__tests__/Button.test.tsx
    src/__tests__/Toggle.test.tsx
    src/__tests__/SubtaskList.test.tsx
    src/__tests__/ProgressRing.test.tsx
    src/__tests__/ProtocolList.test.tsx
    src/__tests__/Skeleton.test.tsx
    src/__tests__/Timeline.test.tsx
  </files>
  <action>
Create test files following the established pattern in `FAB.test.tsx` and `Timeline.test.tsx` (vi.mock motion/react, @testing-library/react render + screen).

**Button.test.tsx** (INTR-01):
- Mock `motion/react-m` to return passthrough elements (same pattern as FAB.test.tsx)
- Test: "renders as m.button with whileTap prop" — render Button, query the rendered element, assert it was created via the mocked m.button factory. Since we mock m.button to render a plain button, assert the component passes whileTap-related props through. Alternatively, test structurally: render Button, assert no `transition-transform` or `active:scale` in className.
- Test: "disabled button does not receive whileTap" — render `<Button disabled>`, assert whileTap is undefined/not passed
- Test: "preserves variant and size classes" — render each variant/size, assert expected className substrings

**Toggle.test.tsx** (INTR-01):
- Test: "renders track as m.button" — render Toggle, assert the switch role element exists
- Test: "no CSS transition-transform on knob" — render Toggle, query the knob span, assert className does NOT include `transition-transform`

**SubtaskList.test.tsx** (INTR-01):
- Test: "renders subtask buttons" — render with sample subtasks, assert buttons rendered
- Test: "checkbox buttons have whileTap" — after migration, buttons should be m.button; test structurally that no `transition-transform` or `active:scale` in className

**ProgressRing.test.tsx** (DATV-01):
- Test: "renders SVG with circle elements" — render ProgressRing with progress=50, assert svg and circle elements exist
- Test: "foreground circle does not have transition-all CSS class" — after migration, the CSS transition is removed

**ProtocolList.test.tsx** (DATV-02):
- Test: "renders protocol items" — render with sample protocols array, assert card count matches
- Test: "list container wraps items" — structural test for wrapper div existence

**Skeleton.test.tsx** (DATV-04):
- Test: "renders with skeleton-shimmer class" — render Skeleton, assert `skeleton-shimmer` in className
- Test: "has aria-hidden=true" — accessibility
- Test: "applies shape variant classes" — render each variant (text, circle, card, block), assert rounded class

**Timeline.test.tsx** (DATV-02 — extend existing):
- Add test: "timeline container wraps task items" — verify the list wrapper div exists with expected structure

All tests should be written to PASS against the CURRENT (pre-migration) code where possible. Tests that verify post-migration state (like "renders as m.button") should be marked with `it.skip` and a comment `// Unskip after Plan 01/02 migration`. This way the suite stays green in Wave 0.

For Skeleton.test.tsx, since the component doesn't exist yet, write the tests with `it.skip` — they'll be unskipped when Plan 02 creates the component.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 7 test files exist. `npx vitest run` passes (skipped tests don't count as failures). Each file has at least 2 test cases.</done>
</task>

<task type="auto">
  <name>Task 2: Create useCountUp hook test</name>
  <files>src/__tests__/useCountUp.test.ts</files>
  <action>
Create `src/__tests__/useCountUp.test.ts` for DATV-03. The hook doesn't exist yet, so all tests are `it.skip`.

Mock `motion/react` to provide controlled `useMotionValue`, `useSpring`, `useMotionValueEvent`:
- `useMotionValue(initial)` returns `{ get: () => val, set: (v) => { val = v } }`
- `useSpring(mv, config)` returns the same motion value (in test, spring is instant)
- `useMotionValueEvent(mv, event, callback)` calls callback immediately with current value

Tests (all `it.skip` with comment `// Unskip after Plan 02 creates hook`):
- "returns 0 initially when target is 0"
- "returns target value after spring settles" — set target to 42, call the change callback, assert display === 42
- "rounds to integer" — set target to 3.7, assert display === 4
- "updates when target changes" — change target from 10 to 20, assert display updates

Import path will be `../../hooks/useCountUp` — the hook file doesn't exist yet.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run src/__tests__/useCountUp.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>useCountUp.test.ts exists with 4 skipped test cases. Suite passes.</done>
</task>

</tasks>

<verification>
All test files created, suite green:
```bash
npx vitest run --reporter=verbose
```
</verification>

<success_criteria>
- 8 test files exist (7 new + 1 extended)
- `npx vitest run` exits 0
- Each file has 2+ test cases (some skipped, none failing)
- Skipped tests have clear comments indicating which plan will unskip them
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-primitives-animations/03-00-SUMMARY.md`
</output>
