---
phase: 03-ui-primitives-animations
plan: 01
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Toggle.tsx
  - src/components/ui/Card.tsx
  - src/components/schedule/SubtaskList.tsx
  - src/components/schedule/TaskBlock.tsx
  - src/__tests__/Button.test.tsx
  - src/__tests__/Toggle.test.tsx
  - src/__tests__/SubtaskList.test.tsx
autonomous: true
requirements:
  - INTR-01
  - INTR-05

must_haves:
  truths:
    - "Tapping any button produces a visible spring-scale response"
    - "Tapping a toggle animates the knob with spring physics"
    - "Tapping a Card with onClick gives spring-scale feedback"
    - "Tapping subtask checkboxes gives spring-scale feedback"
    - "No CSS transition-transform or active:scale remains on any migrated element"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "m.button with whileTap"
      contains: "m.button"
    - path: "src/components/ui/Toggle.tsx"
      provides: "m.button track + spring-driven knob"
      contains: "m.button"
    - path: "src/components/ui/Card.tsx"
      provides: "m.div with whileTap for clickable cards"
      contains: "m.div"
    - path: "src/components/schedule/SubtaskList.tsx"
      provides: "m.button checkboxes with whileTap"
      contains: "m.button"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/motion/transitions.ts"
      via: "import snappy"
      pattern: "import.*snappy.*from.*transitions"
    - from: "src/components/ui/Toggle.tsx"
      to: "src/motion/transitions.ts"
      via: "import snappy"
      pattern: "import.*snappy.*from.*transitions"
---

<objective>
Migrate all interactive UI primitives from CSS transitions to Motion spring-physics tap feedback.

Purpose: INTR-01 (all buttons/toggles/checkboxes show micro-interaction feedback) and INTR-05 (spring physics, no linear/ease).
Output: Button, Toggle, Card, SubtaskList, and TaskBlock complete button all use `m.button`/`m.div` with `whileTap`.
</objective>

<execution_context>
@/Users/samarceneaux/.claude/get-shit-done/workflows/execute-plan.md
@/Users/samarceneaux/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ui-primitives-animations/03-CONTEXT.md
@.planning/phases/03-ui-primitives-animations/03-RESEARCH.md

<interfaces>
<!-- Existing animation primitives (use directly, do not modify) -->

From src/motion/transitions.ts:
```typescript
export const snappy: Transition = { type: 'spring', stiffness: 400, damping: 28, mass: 1 }
export const gentle: Transition = { type: 'spring', stiffness: 180, damping: 24, mass: 1 }
export const instant: Transition = { type: 'spring', stiffness: 600, damping: 40, mass: 0.8 }
```

From src/motion/variants.ts:
```typescript
export const tap: Variants = { whileTap: { scale: 0.97, transition: snappy } }
export const completePulse: Variants = { initial: { scale: 1 }, animate: { scale: [1, 1.05, 1], transition: snappy } }
```

Established import pattern (from FAB.tsx):
```typescript
import * as m from 'motion/react-m'
import { snappy } from '../../motion/transitions'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Button, Toggle, and Card to Motion</name>
  <files>
    src/components/ui/Button.tsx
    src/components/ui/Toggle.tsx
    src/components/ui/Card.tsx
  </files>
  <action>
**Button.tsx:**
1. Add `import * as m from 'motion/react-m'` and `import { snappy } from '../../motion/transitions'`
2. Replace `<button>` with `<m.button>` in the return
3. Add `whileTap={!props.disabled ? { scale: 0.97, transition: snappy } : undefined}` — per locked decision, use inline object (NOT spread from tap variant)
4. Remove `transition-colors duration-150` from className — Motion doesn't animate color, but this CSS transition isn't needed when Motion handles the interaction feel. Actually per RESEARCH: keep `transition-colors` (Motion doesn't own color), only remove `transition-transform` if present. Button.tsx has `transition-colors duration-150` — KEEP it. There's no `transition-transform` or `active:scale` to remove.
5. The `forwardRef` pattern works with `m.button` — motion v12 + React 19 accepts `HTMLButtonElement` ref directly on `m.button`. Keep existing `forwardRef<HTMLButtonElement, ButtonProps>` signature unchanged.

**Toggle.tsx:**
1. Add `import * as m from 'motion/react-m'` and `import { snappy } from '../../motion/transitions'`
2. Replace the track `<button>` with `<m.button>` and add `whileTap={{ scale: 0.97, transition: snappy }}`
3. Replace the knob `<span>` with `<m.span>`. Animate the knob position using `animate={{ x: checked ? 20 : 0 }}` with `transition={snappy}` instead of CSS `translate-x-5`/`translate-x-0`
4. Remove from knob: `transition-transform duration-200` and the conditional `translate-x-5`/`translate-x-0` classes
5. Remove from track: `transition-colors duration-200` — replace with `animate={{ backgroundColor: checked ? 'var(--color-bamboo)' : 'var(--color-surface-overlay)' }}` and `transition={snappy}` on the `m.button`. Remove the conditional `bg-bamboo`/`bg-surface-overlay` classes since Motion owns the color now.

**Card.tsx:**
1. Add `import * as m from 'motion/react-m'` and `import { snappy } from '../../motion/transitions'`
2. When `onClick` is provided, render `<m.div>` with `whileTap={{ scale: 0.98, transition: snappy }}` (0.98 not 0.97 — cards are larger, subtler scale feels better)
3. When no `onClick`, render plain `<div>` (no Motion overhead for static cards)
4. Remove `active:scale-[0.98] transition-transform duration-100` from the onClick conditional classes — Motion owns this now. Keep `cursor-pointer`.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run src/__tests__/Button.test.tsx src/__tests__/Toggle.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Button renders as m.button with whileTap. Toggle track is m.button, knob is m.span with spring-driven position. Card uses m.div with whileTap when clickable. No CSS transition-transform or active:scale on any of these elements.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate SubtaskList and TaskBlock complete button</name>
  <files>
    src/components/schedule/SubtaskList.tsx
    src/components/schedule/TaskBlock.tsx
    src/__tests__/Button.test.tsx
    src/__tests__/Toggle.test.tsx
    src/__tests__/SubtaskList.test.tsx
  </files>
  <action>
**SubtaskList.tsx:**
1. Add `import * as m from 'motion/react-m'` and `import { snappy } from '../../motion/transitions'`
2. Replace each subtask `<button>` with `<m.button>` and add `whileTap={{ scale: 0.97, transition: snappy }}`
3. Remove `transition-colors` from the button className (it was `hover:bg-charcoal/30 transition-colors`) — keep `hover:bg-charcoal/30` for mouse hover, remove `transition-colors` since we want spring-driven feel
4. On the checkbox div inside each button: when `isDone` transitions to true, add a quick pulse. Wrap the checkbox div in `<m.div>` with `animate={isDone ? { scale: [1, 1.08, 1] } : { scale: 1 }}` and `transition={snappy}` — this gives the `completePulse` effect per locked decisions
5. Remove `transition-all duration-150` from the checkbox div className

**TaskBlock.tsx:**
1. The complete button (the circle button with checkmark) currently has `active:scale-90 transition-all duration-200` — remove both
2. Replace that `<button>` with `<m.button>` (import already exists in TaskBlock.tsx since it uses `m.polyline`)
3. Add `whileTap={{ scale: 0.9, transition: snappy }}` — the 0.9 scale matches the existing feel (was `active:scale-90`) but now uses spring physics
4. The edit/duplicate buttons in expanded view: also replace with `<m.button>` + `whileTap={{ scale: 0.97, transition: snappy }}`

**Unskip tests:** In Button.test.tsx, Toggle.test.tsx, and SubtaskList.test.tsx, unskip all `.skip` tests that verify the Motion migration. Update assertions if needed to match actual rendered output.

Run `npx vitest run` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/samarceneaux/Desktop/EVO/Biohacking_CLAUDE_DEV/Dabbling_DEV/時間の流れ && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Every interactive element in SubtaskList and TaskBlock uses m.button with whileTap. No CSS `active:scale-*` or `transition-transform` remains. All tests pass including unskipped INTR-01 tests.</done>
</task>

</tasks>

<verification>
Full test suite green:
```bash
npx vitest run --reporter=verbose
```
Manual spot check: `npx vite dev`, tap buttons/toggles/cards/checkboxes — all should show spring-scale response.
</verification>

<success_criteria>
- Button.tsx, Toggle.tsx, Card.tsx, SubtaskList.tsx, TaskBlock.tsx all use m.* elements with whileTap
- Zero CSS `transition-transform`, `active:scale-*` on migrated elements
- Toggle knob position animated via Motion spring, not CSS translate class
- FAB.tsx unchanged (already migrated)
- All INTR-01 and INTR-05 related tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ui-primitives-animations/03-01-SUMMARY.md`
</output>
